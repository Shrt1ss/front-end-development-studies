'use strict'

/* function Calculator() {
    this.read = function (a, b) {
        a = prompt('Enter A','');
            a = +a;
        b = prompt('Enter B','');
            b = +b;

        this.A = a;
        this.B = b;
    },
    this.sum = function() {
        return this.A + this.B;
    },
    this.mul = function() {
        return this.A * this.B;
    }
};

let calculator = new Calculator();
calculator.read();

console.log( "SUM: " + calculator.sum() );
console.log( "MULTIPLICATION: " + calculator.mul() ); */

/* function Accumulator(startingValue) {
    this.value = startingValue;

    this.read = function() {
        this.value += +prompt('Enter any number','');
    };
};

let accumulator = new Accumulator(1);
let container = new Accumulator(500);

container.read();
container.read();
container.read();

console.log( container.value );

accumulator.read();
accumulator.read();

console.log( accumulator.value ); */